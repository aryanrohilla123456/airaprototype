node_modules/
dist/
build/
.cache/
.env*
.vite/
vite.config.*.timestamp-*
*.tsbuildinfo
*-debug.log*
logs/
*.log
.claude-replay.*
.tmp-*

# Platform-managed agentplat state, not project source. In the
# client-initiated single-repo workspace layout (make's `Path: "."` spec) the
# repo root IS the workspace root, so these land inside this repo rather than
# beside it.
/.plugins/
/.opencode-skills/
 
# Ensure essential source files are not ignored (make repo contents visible)
!/.gitignore
!/index.html
!/package.json
!/package-lock.json
!/pnpm-lock.yaml
!/tsconfig.json
!/vite.config.ts
!/src/
!/src/**
